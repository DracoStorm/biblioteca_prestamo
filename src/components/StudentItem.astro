---
interface Props {
    name: string;
    matricula: string;
    email: string;
}

const { name, matricula, email } = Astro.props;
---

<div>
    <a href="/admin/loan/update" id="matricula">󰺄</a>
    <span id="name">{name}</span>
    <span id="matricula">{matricula}</span>
    <span id="email">{email}</span>
</div>

<style>

@font-face {
  font-family: 'Arvo';
  src: url('/fonts/Arvo/Arvo-Regular.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

    div {
        display: flex;
        background-color: #443659;
        text-align: center;
        border-radius: 0.5rem;
        overflow: hidden;
        height: 3rem;
        line-height: 2.4;
        font-size: 1.2rem;
        font-family: 'Arvo', sans-serif;
    }
    span {
        flex-grow: 1;
    }
    #matricula {
        background-color: #1116;
    }
    
    a {
        background-color: #111a;
        border-radius: 0;
        color: #ebeaea;
        font-size: 2rem;
        font-weight: bold;
        padding: 0.2rem 1rem;
        text-decoration: none;
        line-height: 1.2;
    }

    
</style>
