---
interface Props {
    message: string;
}

const { message } = Astro.props;
---

<div>
    <span id="message">{message}</span>
    <a href="loan" id="matricula"></a>
</div>

<style>
    div {
        display: flex;
        background-color: #59363697;
        text-align: center;
        border-radius: 0.5rem;
        overflow:hidden
        height: 3rem;
        line-height: 2.4;
        font-size: 2rem;
        margin: 5rem;
        table-layout: fixed;
        text-align: center;
        white-space: nowrap;
        vertical-align: middle;
        height: 5rem;
    }
    span {
        flex-grow: 1;
    }
    #matricula {
        background-color: #1116;
    }

    a {
        background-color: #111a;
        border-radius: 0;
        color: #ebeaea;
        font-size: 3.5rem;
        font-weight: bold;
        padding: 0.4rem 2rem;
        text-decoration: none;
        line-height: 1.2;
        table-layout: fixed;
        width: 6rem;
    }
</style>
