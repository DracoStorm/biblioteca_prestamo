---
import Layout from "../../../layouts/Layout.astro";
import Nav from "../../../components/NavBarAdmin.astro";
import SearchBar from "../../../components/SearchBarAdminStudent.svelte";

// Checa si existe la sesión
const token = Astro.cookies.get("token");
if (token) {
    console.log(token);
} else return Astro.redirect("/");
---

<Layout title="modificacion prestamo">
    <Nav />
    <SearchBar client:load cookies={token.value} />
</Layout>
