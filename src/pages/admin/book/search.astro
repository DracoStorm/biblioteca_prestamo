---
import Layout from "../../../layouts/Layout.astro";
import NavUser from "../../../components/NavBarAdmin.astro";
import SearchBar from "../../../components/SearchBarAdminBook.svelte";

// Checa si existe la sesión
const token = Astro.cookies.get("token");
if (token) {
    console.log(token);
} else return Astro.redirect("/");
---

<Layout title="Buscar libro">
    <NavUser />
    <SearchBar client:load cokies={token.value} />
    <footer>.</footer>
</Layout>
